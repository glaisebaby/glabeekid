{
  "name": "@glabeekid/keepalive-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare cron worker that keeps the Render backend warm on a configurable schedule.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "4.120.1"
  }
}
